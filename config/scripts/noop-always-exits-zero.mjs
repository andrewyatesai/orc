#!/usr/bin/env node
// Fixture for gate-negative-coverage: a script that always succeeds, used to prove
// assertGateRejects throws SYNCHRONOUSLY when a gate accepts a planted violation.
process.exit(0)
