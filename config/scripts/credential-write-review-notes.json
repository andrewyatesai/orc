{
  "//": [
    "Reviewed classifications for credential write sites, produced by a manual security review and carried forward.",
    "This file ANNOTATES the output of report:credential-writes. It cannot add, remove, hide or reclassify a site.",
    "The reporter prints every site it finds; a site with no entry here prints as \"unreviewed\", and an entry here",
    "that matches no site prints in the STALE REVIEW NOTES section. Deleting this whole file changes nothing except",
    "that every site prints as \"unreviewed\".",
    "Keys are file|enclosing-declaration|sink|ordinal - not line numbers - so ordinary edits do not churn this file.",
    "",
    "VERDICTS (what the reviewer concluded, not what the tool proved)",
    "  cleartext-fallback-unfixed     REAL FINDING. A secret is written in cleartext when safeStorage is unavailable, with no plaintext-policy opt-in. These are the ones to fix.",
    "  gated-dev-plaintext-opt-in     Cleartext, but only reachable behind a sanctioned plaintext-policy predicate that refuses in packaged/production builds. The report shows these as gated-by=.",
    "  deliberate-plaintext-interop   An external CLI owns the on-disk format and reads it back; Orca cannot encrypt it. Mitigated with 0600 + ownership checks.",
    "  locally-minted-handshake-token A random token Orca itself mints for a local handshake; it must be readable at rest for its only purpose.",
    "  encrypted-payload              The bytes written are safeStorage ciphertext or a sealed envelope; only the surrounding names matched the vocabulary.",
    "  non-secret-payload             Reviewed: the written bytes contain no secret (metadata, pid, diagnostics, screenshot).",
    "  orchestrator-forwarding        A call into a routine that writes further down; the write is classified at its own site.",
    "  fd-write-not-a-file            An fd-level write whose destination is a pipe, which the sink model cannot distinguish from a file."
  ],
  "sites": {
    "src/cli/handlers/browser-cookie.ts|BROWSER_COOKIE_HANDLERS|wrapper:prepareComputerCliJsonResult:printResult|0": {
      "verdict": "non-secret-payload",
      "reason": "printResult -> prepareComputerCliJsonResult writes only a screenshot image to a temp file; the cookie-named response fields go to stdout, not to disk."
    },
    "src/cli/handlers/browser-cookie.ts|BROWSER_COOKIE_HANDLERS|wrapper:prepareComputerCliJsonResult:printResult|1": {
      "verdict": "non-secret-payload",
      "reason": "printResult -> prepareComputerCliJsonResult writes only a screenshot image to a temp file; the cookie-named response fields go to stdout, not to disk."
    },
    "src/cli/handlers/browser-cookie.ts|BROWSER_COOKIE_HANDLERS|wrapper:prepareComputerCliJsonResult:printResult|2": {
      "verdict": "non-secret-payload",
      "reason": "printResult -> prepareComputerCliJsonResult writes only a screenshot image to a temp file; the cookie-named response fields go to stdout, not to disk."
    },
    "src/cli/handlers/browser-env.ts|BROWSER_ENV_HANDLERS|wrapper:prepareComputerCliJsonResult:printResult|0": {
      "verdict": "non-secret-payload",
      "reason": "printResult -> prepareComputerCliJsonResult writes only a screenshot image to a temp file; the cookie-named response fields go to stdout, not to disk."
    },
    "src/main/agent-hooks/managed-hook-lock-claims.ts|claimManagedHookLock|fs/promises:fs.promises:writeFile|0": {
      "verdict": "locally-minted-handshake-token",
      "reason": "A per-process randomUUID lock/owner token, written O_EXCL 0600 into a uid-checked directory. The token exists only to prove ownership of the lock; it grants nothing off-host and has no value to encrypt."
    },
    "src/main/agent-hooks/managed-hook-lock-records.ts|tryCreateManagedHookLock|fs/promises:fs.promises:writeFile|0": {
      "verdict": "locally-minted-handshake-token",
      "reason": "A per-process randomUUID lock/owner token, written O_EXCL 0600 into a uid-checked directory. The token exists only to prove ownership of the lock; it grants nothing off-host and has no value to encrypt."
    },
    "src/main/agent-hooks/managed-hook-owner-identity.ts|readDurableHostToken|fs/promises:fs.promises:writeFile|0": {
      "verdict": "locally-minted-handshake-token",
      "reason": "A per-process randomUUID lock/owner token, written O_EXCL 0600 into a uid-checked directory. The token exists only to prove ownership of the lock; it grants nothing off-host and has no value to encrypt."
    },
    "src/main/agent-hooks/server.ts|AgentHookServer.maybeWriteEndpointFile.ok|wrapper:writeFileSync:writeEndpointFile|0": {
      "verdict": "locally-minted-handshake-token",
      "reason": "The agent-hook endpoint file publishes a per-run random loopback token that the agent CLI must source from a shell. Encrypting it would defeat its only purpose; it is short-lived, loopback-scoped and shell-safety validated before the write."
    },
    "src/main/browser/browser-cookie-import.ts|getLinuxEncryptionKey|wrapper:appendFileSync:diag|0": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromBrowser|wrapper:appendFileSync:diag|0": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromBrowser|wrapper:appendFileSync:diag|1": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromBrowser|wrapper:appendFileSync:diag|10": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends one line to the cookie-import debug log. The bytes are counts, partition ids and cookie DOMAIN names \u2014 never a cookie value; the module deliberately keeps even that off stdout unless ORCA_COOKIE_IMPORT_DEBUG is set. \"cookies\" is matched in the template text, which the detector now assembles."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromBrowser|wrapper:appendFileSync:diag|2": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromBrowser|wrapper:appendFileSync:diag|3": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromBrowser|wrapper:appendFileSync:diag|4": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromBrowser|wrapper:appendFileSync:diag|5": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromBrowser|wrapper:appendFileSync:diag|6": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends one line to the cookie-import debug log. The bytes are counts, partition ids and cookie DOMAIN names \u2014 never a cookie value; the module deliberately keeps even that off stdout unless ORCA_COOKIE_IMPORT_DEBUG is set. \"cookies\" is matched in the template text, which the detector now assembles."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromBrowser|wrapper:appendFileSync:diag|7": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends one line to the cookie-import debug log. The bytes are counts, partition ids and cookie DOMAIN names \u2014 never a cookie value; the module deliberately keeps even that off stdout unless ORCA_COOKIE_IMPORT_DEBUG is set. \"cookies\" is matched in the template text, which the detector now assembles."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromBrowser|wrapper:appendFileSync:diag|8": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends one line to the cookie-import debug log. The bytes are counts, partition ids and cookie DOMAIN names \u2014 never a cookie value; the module deliberately keeps even that off stdout unless ORCA_COOKIE_IMPORT_DEBUG is set. \"cookies\" is matched in the template text, which the detector now assembles."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromBrowser|wrapper:appendFileSync:diag|9": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends one line to the cookie-import debug log. The bytes are counts, partition ids and cookie DOMAIN names \u2014 never a cookie value; the module deliberately keeps even that off stdout unless ORCA_COOKIE_IMPORT_DEBUG is set. \"cookies\" is matched in the template text, which the detector now assembles."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromBrowser|wrapper:diag:disableStaging|0": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromBrowser|wrapper:persistMeta:setPendingCookieImport|0": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromFirefox|wrapper:appendFileSync:diag|0": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromFirefox|wrapper:appendFileSync:diag|1": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends one line to the cookie-import debug log. The bytes are counts, partition ids and cookie DOMAIN names \u2014 never a cookie value; the module deliberately keeps even that off stdout unless ORCA_COOKIE_IMPORT_DEBUG is set. \"cookies\" is matched in the template text, which the detector now assembles."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromFirefox|wrapper:diag:importValidatedCookies|0": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromSafari|wrapper:appendFileSync:diag|0": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromSafari|wrapper:appendFileSync:diag|1": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends one line to the cookie-import debug log. The bytes are counts, partition ids and cookie DOMAIN names \u2014 never a cookie value; the module deliberately keeps even that off stdout unless ORCA_COOKIE_IMPORT_DEBUG is set. \"cookies\" is matched in the template text, which the detector now assembles."
    },
    "src/main/browser/browser-cookie-import.ts|importCookiesFromSafari|wrapper:diag:importValidatedCookies|0": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importValidatedCookies|wrapper:appendFileSync:diag|0": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importValidatedCookies|wrapper:appendFileSync:diag|1": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-cookie-import.ts|importValidatedCookies|wrapper:appendFileSync:diag|2": {
      "verdict": "non-secret-payload",
      "reason": "diag() appends timestamped progress lines (browser family, partition name, counts) to a 0600 diagnostics log opened O_NOFOLLOW. Cookie values are never interpolated into the message."
    },
    "src/main/browser/browser-session-registry.ts|BrowserSessionRegistry.applyPendingCookieImport|wrapper:writeFileSync:persistMeta|0": {
      "verdict": "non-secret-payload",
      "reason": "persistMeta writes profile metadata (ids, labels, a pending-import marker). Cookie material lives in the Electron session store, not in this file."
    },
    "src/main/browser/browser-session-registry.ts|BrowserSessionRegistry.clearDefaultSessionCookies|wrapper:writeFileSync:persistMeta|0": {
      "verdict": "non-secret-payload",
      "reason": "persistMeta writes profile metadata (ids, labels, a pending-import marker). Cookie material lives in the Electron session store, not in this file."
    },
    "src/main/browser/browser-session-registry.ts|BrowserSessionRegistry.clearPendingCookieImport|wrapper:writeFileSync:persistMeta|0": {
      "verdict": "non-secret-payload",
      "reason": "persistMeta writes profile metadata (ids, labels, a pending-import marker). Cookie material lives in the Electron session store, not in this file."
    },
    "src/main/browser/browser-session-registry.ts|BrowserSessionRegistry.deleteProfile|wrapper:writeFileSync:persistMeta|0": {
      "verdict": "non-secret-payload",
      "reason": "persistMeta writes profile metadata (ids, labels, a pending-import marker). Cookie material lives in the Electron session store, not in this file."
    },
    "src/main/browser/browser-session-registry.ts|BrowserSessionRegistry.setPendingCookieImport|wrapper:writeFileSync:persistMeta|0": {
      "verdict": "non-secret-payload",
      "reason": "persistMeta writes profile metadata (ids, labels, a pending-import marker). Cookie material lives in the Electron session store, not in this file."
    },
    "src/main/claude-accounts/managed-auth-path.ts|resolveOwnedClaudeManagedAuthPath|fs:fs:writeFileSync|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/managed-auth-path.ts|writeClaudeManagedAuthFile|wrapper:writeFileSync:writeFileAtomically|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.captureSystemDefaultSnapshotForManagedEntry|wrapper:writeJson:captureSystemDefaultSnapshot|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.captureSystemDefaultSnapshotForManagedEntry|wrapper:writeJson:captureSystemDefaultSnapshot|1": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.captureSystemDefaultSnapshot|wrapper:writeFileAtomically:writeJson|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.doSyncForCurrentSelection.refreshed|wrapper:writeManagedCredentials:refreshManagedAccountTokenIfNeeded|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.doSyncForCurrentSelection|wrapper:getOwnedManagedAuthPath:writeManagedCredentials|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.doSyncForCurrentSelection|wrapper:getOwnedManagedAuthPath:writeManagedCredentials|1": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.doSyncForCurrentSelection|wrapper:writeFileAtomically:writeRuntimeCredentials|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.getOwnedManagedAuthPath|wrapper:writeFileSync:resolveOwnedClaudeManagedAuthPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.refreshManagedAccountTokenIfNeeded|wrapper:getOwnedManagedAuthPath:writeManagedCredentials|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.restoreRuntimeCredentials|wrapper:writeFileAtomically:writeRuntimeCredentials|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.restoreSystemDefaultSnapshotForMissingManagedCredentials|wrapper:writeRuntimeCredentials:restoreRuntimeCredentials|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.restoreSystemDefaultSnapshot|wrapper:writeRuntimeCredentials:restoreRuntimeCredentials|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.writeManagedCredentials|wrapper:writeFileAtomically:writeClaudeManagedAuthFile|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/runtime-auth-service.ts|ClaudeRuntimeAuthService.writeRuntimeCredentials|wrapper:writeFileSync:writeFileAtomically|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.assertManagedAuthPath.trustedPath|wrapper:writeFileSync:resolveOwnedClaudeManagedAuthPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.createManagedAuthDir.managedAuthPath|wrapper:resolveOwnedClaudeManagedAuthPath:assertManagedAuthPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.createManagedAuthDir|fs:fs:writeFileSync|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.doAddAccount|wrapper:assertManagedAuthPath:safeRemoveManagedAuth|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.doAddAccount|wrapper:writeManagedCredentials:writeManagedAuth|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.doReauthenticateAccount.managedAuthPath|wrapper:resolveOwnedClaudeManagedAuthPath:assertManagedAuthPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.doReauthenticateAccount.previousManagedAuth|wrapper:assertManagedAuthPath:readManagedAuthSnapshot|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.doReauthenticateAccount|wrapper:assertManagedAuthPath:restoreManagedCredentialsSnapshot|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.doReauthenticateAccount|wrapper:assertManagedAuthPath:restoreManagedOauthSnapshot|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.doReauthenticateAccount|wrapper:assertManagedAuthPath:writeManagedCredentials|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.doReauthenticateAccount|wrapper:assertManagedAuthPath:writeManagedOauthAccount|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.doRemoveAccount|wrapper:assertManagedAuthPath:safeRemoveManagedAuth|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.readManagedAuthSnapshot.trustedPath|wrapper:resolveOwnedClaudeManagedAuthPath:assertManagedAuthPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.restoreManagedCredentialsSnapshot.trustedPath|wrapper:resolveOwnedClaudeManagedAuthPath:assertManagedAuthPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.restoreManagedCredentialsSnapshot|wrapper:writeFileAtomically:writeClaudeManagedAuthFile|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.restoreManagedOauthSnapshot.trustedPath|wrapper:resolveOwnedClaudeManagedAuthPath:assertManagedAuthPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.restoreManagedOauthSnapshot|wrapper:writeFileAtomically:writeClaudeManagedAuthFile|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.tryCreateWslManagedAuthDir.managedAuthPath|wrapper:resolveOwnedClaudeManagedAuthPath:assertManagedAuthPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.writeManagedAuth|wrapper:assertManagedAuthPath:writeManagedCredentials|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.writeManagedAuth|wrapper:assertManagedAuthPath:writeManagedOauthAccount|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.writeManagedCredentials.trustedPath|wrapper:resolveOwnedClaudeManagedAuthPath:assertManagedAuthPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.writeManagedCredentials|wrapper:writeFileAtomically:writeClaudeManagedAuthFile|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.writeManagedOauthAccount.trustedPath|wrapper:resolveOwnedClaudeManagedAuthPath:assertManagedAuthPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/claude-accounts/service.ts|ClaudeAccountService.writeManagedOauthAccount|wrapper:writeFileAtomically:writeClaudeManagedAuthFile|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: .credentials.json / oauth-account.json are the Claude CLI's own on-disk format. The external CLI reads and refreshes these paths; Orca does not own the format and cannot encrypt them without breaking the CLI. Mitigations in code: 0600 mode, symlink refusal (lstat + realpath), ownership marker under the managed root, atomic tmp+rename."
    },
    "src/main/codex-accounts/legacy-shared-auth-migration.ts|migrateLegacySharedAuthToPerAccountHome|wrapper:writeFileAtomically:migrateSharedMcpCredentials|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/legacy-shared-auth-migration.ts|migrateLegacySharedAuthToPerAccountHome|wrapper:writeFileSync:writeFileAtomically|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/legacy-shared-auth-migration.ts|migrateSharedMcpCredentials|wrapper:writeFileSync:writeFileAtomically|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.captureSystemDefaultSnapshot|wrapper:writeFileSync:writeFileAtomically|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.persistRuntimeLogoutMarker|wrapper:writeFileSync:writeFileAtomically|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.readBackActiveWslAccountBeforeRestart|wrapper:writeFileAtomically:readBackRefreshedTokensFromPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.readBackRefreshedTokens.selectedAccountResult|wrapper:writeFileAtomically:readBackRefreshedTokensFromPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.readBackRefreshedTokensForAccount|wrapper:writeFileAtomically:readBackRefreshedTokensFromPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.readBackRefreshedTokensFromPath|wrapper:writeFileSync:writeFileAtomically|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.readBackRefreshedTokens|wrapper:writeFileAtomically:readBackRefreshedTokensFromPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.recoverRefreshForMissingActiveAccount|wrapper:writeFileSync:writeFileAtomically|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.restoreSystemDefaultSnapshot|wrapper:writeFileAtomically:writeRuntimeAuth|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.restoreSystemDefaultSnapshot|wrapper:writeFileAtomically:writeRuntimeAuth|1": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.restoreSystemDefaultSnapshot|wrapper:writeFileAtomically:writeRuntimeAuth|2": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.syncForCurrentSelection|wrapper:writeFileAtomically:writeRuntimeAuth|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.syncRuntimeAuthWithSystemDefault|wrapper:writeFileAtomically:writeRuntimeAuth|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.syncRuntimeAuthWithSystemDefault|wrapper:writeFileAtomically:writeSystemDefaultAuth|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.syncWslRuntimeForCurrentSelection|wrapper:writeFileAtomically:readBackRefreshedTokensFromPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.syncWslRuntimeForCurrentSelection|wrapper:writeFileAtomically:writeRuntimeAuthAtPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.syncWslRuntimeForCurrentSelection|wrapper:writeFileAtomically:writeRuntimeAuthAtPath|1": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.syncWslRuntimeForCurrentSelection|wrapper:writeFileAtomically:writeRuntimeAuthAtPath|2": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.writeRuntimeAuthAtPath|wrapper:writeFileSync:writeFileAtomically|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.writeRuntimeAuth|wrapper:writeFileSync:writeFileAtomically|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.writeRuntimeAuth|wrapper:writeFileSync:writeFileAtomically|1": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/codex-accounts/runtime-home-service.ts|CodexRuntimeHomeService.writeSystemDefaultAuth|wrapper:writeFileSync:writeFileAtomically|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: auth.json is the Codex CLI's own on-disk format; codex refreshes the OAuth tokens in place. Orca mirrors it 0600 with a compare-and-swap baseline (#9582) so a live PTY refresh is never clobbered."
    },
    "src/main/computer/macos-native-provider-transport.ts|startMacOSNativeProviderSocket|fs:fs:writeFileSync|0": {
      "verdict": "locally-minted-handshake-token",
      "reason": "Per-launch randomUUID handed to the nested signed helper through a 0600 file in a 0700 mkdtemp directory."
    },
    "src/main/daemon/daemon-init.ts|createOutOfProcessLauncher|wrapper:writeDaemonPidFile:launchRustDaemon|0": {
      "verdict": "non-secret-payload",
      "reason": "Daemon pid file: pid, start time, entry path, app version. The vocabulary hit comes from the surrounding launcher, not from the written bytes."
    },
    "src/main/daemon/daemon-init.ts|launchRustDaemon|wrapper:writeDaemonPidFile:backfillWin32DaemonPidFileStartTime|0": {
      "verdict": "non-secret-payload",
      "reason": "Daemon pid file: pid, start time, entry path, app version. The vocabulary hit comes from the surrounding launcher, not from the written bytes."
    },
    "src/main/daemon/daemon-init.ts|launchRustDaemon|wrapper:writeFileSync:writeDaemonPidFile|0": {
      "verdict": "non-secret-payload",
      "reason": "Daemon pid file: pid, start time, entry path, app version. The vocabulary hit comes from the surrounding launcher, not from the written bytes."
    },
    "src/main/daemon/daemon-server.ts|DaemonServer.start|fs:fs:writeFileSync|0": {
      "verdict": "locally-minted-handshake-token",
      "reason": "Per-start random daemon token written 0600 beside the unix socket; the local client must read it to connect."
    },
    "src/main/index.ts|<module>|wrapper:migrateStagingProfileKeychain:migrateStagingProfile|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/integration-credential-file.ts|writeStoredCredentialToken|fs:fs:writeFileSync|0": {
      "verdict": "encrypted-payload",
      "reason": "safeStorage ciphertext branch shared by Jira and Linear: the bytes are the output of safeStorage.encryptString, written 0600 to the .enc path, and any stale .plaintext sibling is removed right after."
    },
    "src/main/integration-credential-file.ts|writeStoredCredentialToken|fs:fs:writeFileSync|1": {
      "verdict": "gated-dev-plaintext-opt-in",
      "reason": "Fixed: the Jira/Linear cleartext fallback is now selected only by allowsPlaintextPersistedSecret, written 0600 to a .plaintext sibling (never .enc) with the orca-plaintext-v1: tag so a reader can tell it from ciphertext. Without the opt-in nothing is written at all \u2014 the token is kept in memory for the session and any superseded file on disk is deleted."
    },
    "src/main/integration-credential-file.ts|upgradeStoredCredentialToCiphertext|fs:fs:writeFileSync|0": {
      "verdict": "encrypted-payload",
      "reason": "Writes safeStorage ciphertext only; it runs to REPLACE a cleartext token left by an older build once a keychain is available again, then unlinks the cleartext file."
    },
    "src/main/integration-credential-file.ts|readStoredCredentialTokenFile|wrapper:writeFileSync:upgradeStoredCredentialToCiphertext|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "Calls upgradeStoredCredentialToCiphertext, whose ciphertext write is classified at its own site."
    },
    "src/main/ipc/jira.ts|registerJiraHandlers.result|wrapper:saveToken:connect|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/ipc/linear.ts|registerLinearHandlers.result|wrapper:saveWorkspaceToken:connect|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/ipc/minimax-credentials.ts|registerMiniMaxCredentialsHandlers|wrapper:writeSecureFile:saveMiniMaxSessionCookie|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/ipc/pty.ts|registerPtyHandlers.spawn|wrapper:ensureLinuxTerminalOrcaCliShimDir:buildPtyHostEnv|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/ipc/pty.ts|registerPtyHandlers|wrapper:ensureLinuxTerminalOrcaCliShimDir:buildPtyHostEnv|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/ipc/runtime.ts|registerRuntimeHandlers|wrapper:emulatorProbe:dispatch|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/ipc/speech.ts|registerSpeechHandlers|wrapper:writeFileSync:saveOpenAiSpeechApiKey|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/jira/client.ts|readToken.token|wrapper:upgradeStoredCredentialToCiphertext:readStoredCredentialTokenFile|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into writeStoredCredentialToken / readStoredCredentialTokenFile in src/main/integration-credential-file.ts; the write is classified at its own site there."
    },
    "src/main/jira/client.ts|saveToken|wrapper:writeFileSync:writeStoredCredentialToken|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into writeStoredCredentialToken / readStoredCredentialTokenFile in src/main/integration-credential-file.ts; the write is classified at its own site there."
    },
    "src/main/jira/client.ts|connect|wrapper:writeStoredCredentialToken:saveToken|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into writeStoredCredentialToken / readStoredCredentialTokenFile in src/main/integration-credential-file.ts; the write is classified at its own site there."
    },
    "src/main/linear/client.ts|saveWorkspaceToken|wrapper:writeFileSync:writeStoredCredentialToken|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into writeStoredCredentialToken / readStoredCredentialTokenFile in src/main/integration-credential-file.ts; the write is classified at its own site there."
    },
    "src/main/linear/client.ts|saveToken|wrapper:writeStoredCredentialToken:saveWorkspaceToken|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into writeStoredCredentialToken / readStoredCredentialTokenFile in src/main/integration-credential-file.ts; the write is classified at its own site there."
    },
    "src/main/linear/client.ts|loadToken.token|wrapper:upgradeStoredCredentialToCiphertext:readStoredCredentialTokenFile|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into writeStoredCredentialToken / readStoredCredentialTokenFile in src/main/integration-credential-file.ts; the write is classified at its own site there."
    },
    "src/main/linear/client.ts|replaceLegacyWorkspace|wrapper:writeStoredCredentialToken:saveWorkspaceToken|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into writeStoredCredentialToken / readStoredCredentialTokenFile in src/main/integration-credential-file.ts; the write is classified at its own site there."
    },
    "src/main/linear/client.ts|connect|wrapper:writeStoredCredentialToken:saveWorkspaceToken|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into writeStoredCredentialToken / readStoredCredentialTokenFile in src/main/integration-credential-file.ts; the write is classified at its own site there."
    },
    "src/main/linear/client.ts|testConnection|wrapper:saveWorkspaceToken:replaceLegacyWorkspace|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into writeStoredCredentialToken / readStoredCredentialTokenFile in src/main/integration-credential-file.ts; the write is classified at its own site there."
    },
    "src/main/linear/client.ts|testConnection|wrapper:writeStoredCredentialToken:saveWorkspaceToken|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into writeStoredCredentialToken / readStoredCredentialTokenFile in src/main/integration-credential-file.ts; the write is classified at its own site there."
    },
    "src/main/minimax/minimax-cookie-store.ts|saveMiniMaxSessionCookie|wrapper:writeFileSync:writeSecureFile|0": {
      "verdict": "encrypted-payload",
      "reason": "safeStorage ciphertext branch of the MiniMax session-cookie envelope."
    },
    "src/main/minimax/minimax-cookie-store.ts|saveMiniMaxSessionCookie|wrapper:writeFileSync:writeSecureFile|1": {
      "verdict": "gated-dev-plaintext-opt-in",
      "reason": "Fixed: now selected only by allowsPlaintextPersistedSecret and written under a distinguishable 'dev-plaintext' envelope kind. Without the opt-in the cookie is cached in memory and the save throws, so no cleartext reaches disk and the UI cannot report a phantom save."
    },
    "src/main/minimax/minimax-cookie-store.ts|upgradeCleartextCookieAtRest|wrapper:writeFileSync:writeSecureFile|0": {
      "verdict": "encrypted-payload",
      "reason": "Re-encrypts a cleartext cookie already on disk (legacy envelope, pre-envelope raw bytes, or a dev opt-in write) the first time a read finds safeStorage available; the bytes written are safeStorage ciphertext."
    },
    "src/main/minimax/minimax-cookie-store.ts|readMiniMaxSessionCookie|wrapper:writeSecureFile:upgradeCleartextCookieAtRest|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/orca-profiles/profile-cloud-capability-refresh.ts|refreshCurrentOrcaProfileAuth.advanced|wrapper:recordCloudSessionIdentityMutation:recordCloudSessionIdentityMutationIfCurrent|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into the Orca cloud-session store, which performs its own write; that inner write is reported at its own site and is gated by allowsPlaintextOrcaCloudSession."
    },
    "src/main/orca-profiles/profile-cloud-capability-refresh.ts|refreshCurrentOrcaProfileAuth|wrapper:saveOrcaCloudSession:saveOrcaCloudSessionIfCurrent|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into the Orca cloud-session store, which performs its own write; that inner write is reported at its own site and is gated by allowsPlaintextOrcaCloudSession."
    },
    "src/main/orca-profiles/profile-cloud-service.ts|connectCurrentOrcaProfile|wrapper:recordSuccessfulCloudSessionLogin:saveOrcaCloudSessionExchange|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into the Orca cloud-session store, which performs its own write; that inner write is reported at its own site and is gated by allowsPlaintextOrcaCloudSession."
    },
    "src/main/orca-profiles/profile-cloud-service.ts|createCloudLinkedOrcaProfile|wrapper:recordSuccessfulCloudSessionLogin:saveOrcaCloudSessionExchange|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into the Orca cloud-session store, which performs its own write; that inner write is reported at its own site and is gated by allowsPlaintextOrcaCloudSession."
    },
    "src/main/orca-profiles/profile-cloud-service.ts|selectCurrentOrcaProfileOrg.list|wrapper:recordCloudSessionIdentityMutation:selectCloudOrgWithMutationFence|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into the Orca cloud-session store, which performs its own write; that inner write is reported at its own site and is gated by allowsPlaintextOrcaCloudSession."
    },
    "src/main/orca-profiles/profile-cloud-session-refresh.ts|refreshStoredCloudSession.task|wrapper:saveOrcaCloudSession:saveOrcaCloudSessionIfCurrent|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into the Orca cloud-session store, which performs its own write; that inner write is reported at its own site and is gated by allowsPlaintextOrcaCloudSession."
    },
    "src/main/orca-profiles/profile-cloud-session-store.ts|saveOrcaCloudSessionExchange|wrapper:writeSecureJsonFile:saveOrcaCloudSession|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "Calls saveOrcaCloudSession, whose plaintext branch IS gated by allowsPlaintextOrcaCloudSession."
    },
    "src/main/rate-limits/claude-fetcher.ts|fetchManagedAccountUsage|wrapper:writeClaudeManagedAuthFile:writeManagedCredentialsJson|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: mirrors managed Claude credentials into the CLI-owned .credentials.json so `claude` can report usage. Same format constraint as claude-accounts."
    },
    "src/main/rate-limits/claude-fetcher.ts|fetchManagedUsagePanelSupplement|wrapper:writeClaudeManagedAuthFile:writeManagedCredentialsJson|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: mirrors managed Claude credentials into the CLI-owned .credentials.json so `claude` can report usage. Same format constraint as claude-accounts."
    },
    "src/main/rate-limits/claude-fetcher.ts|resolveManagedCredentialsLocation.managedAuthPath|wrapper:writeFileSync:resolveOwnedClaudeManagedAuthPath|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: mirrors managed Claude credentials into the CLI-owned .credentials.json so `claude` can report usage. Same format constraint as claude-accounts."
    },
    "src/main/rate-limits/claude-fetcher.ts|writeManagedCredentialsJson|wrapper:writeFileAtomically:writeClaudeManagedAuthFile|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: mirrors managed Claude credentials into the CLI-owned .credentials.json so `claude` can report usage. Same format constraint as claude-accounts."
    },
    "src/main/rate-limits/gemini-oauth-sources.ts|saveGeminiCredentials|fs/promises:fs.promises:writeFile|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: ~/.gemini/oauth_creds.json is the Gemini CLI's own format; the file is written 0600 via tmp+rename."
    },
    "src/main/rate-limits/gemini-usage-fetcher.ts|fetchGeminiRateLimits.result|wrapper:saveGeminiCredentials:fetchViaOauthCreds|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: ~/.gemini/oauth_creds.json is the Gemini CLI's own format; the file is written 0600 via tmp+rename."
    },
    "src/main/rate-limits/gemini-usage-fetcher.ts|fetchViaOauthCreds|wrapper:writeFile:saveGeminiCredentials|0": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: ~/.gemini/oauth_creds.json is the Gemini CLI's own format; the file is written 0600 via tmp+rename."
    },
    "src/main/rate-limits/gemini-usage-fetcher.ts|fetchViaOauthCreds|wrapper:writeFile:saveGeminiCredentials|1": {
      "verdict": "deliberate-plaintext-interop",
      "reason": "INTEROP: ~/.gemini/oauth_creds.json is the Gemini CLI's own format; the file is written 0600 via tmp+rename."
    },
    "src/main/runtime/device-registry.ts|DeviceRegistry.createAndPersistDevice|wrapper:writeSecureJsonFile:save|0": {
      "verdict": "locally-minted-handshake-token",
      "reason": "Device pairing bearer token (randomBytes(24)) in the 0600-hardened device registry. The runtime must compare it on every paired-device request, so it has to be readable at rest."
    },
    "src/main/runtime/e2ee-keypair-provider.ts|createE2EEKeypairProvider.attempt|wrapper:migratePlaintextEnvelope:resolveE2EEIdentity|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "Calls resolveE2EEIdentity; the write is reported at its own site in e2ee-keypair.ts."
    },
    "src/main/runtime/e2ee-keypair.ts|migratePlaintextEnvelope|wrapper:writeSecureFile:writeSecureJsonFile|0": {
      "verdict": "encrypted-payload",
      "reason": "Writes only the sealed electron-safe-storage-v1 envelope; it runs to REPLACE a plaintext file, never to create one."
    },
    "src/main/runtime/e2ee-keypair.ts|mintKeypair|wrapper:writeSecureFile:writeSecureJsonFile|0": {
      "verdict": "cleartext-fallback-unfixed",
      "reason": "Writes the mobile transport PRIVATE key with secretKeyFormat: \"plaintext\" (0600) when the keychain cannot seal it. Kept, and gated on allowsPlaintextE2EEIdentity() in src/main/runtime/e2ee-identity-plaintext-fallback.ts: this key is a durable identity (the paired phone stores our public half, the relay derives our host id from it), so refusing costs no re-auth - the next launch mints a different identity and silently orphans every paired device. Warned on every mint and on every load that leaves it cleartext, upgraded to the sealed envelope on the next interactive load, and refused outright when an operator sets ORCA_REQUIRE_SEALED_E2EE_IDENTITY=1. Reads as unguarded here on purpose: the sanctioned registry means dev-only opt-in, which a predicate that must say yes in headless production cannot honour."
    },
    "src/main/runtime/e2ee-keypair.ts|resolveE2EEIdentity|wrapper:writeSecureJsonFile:migratePlaintextEnvelope|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "Calls mintKeypair/migratePlaintextEnvelope; the write is reported at its own site inside those functions."
    },
    "src/main/runtime/e2ee-keypair.ts|resolveE2EEIdentity|wrapper:writeSecureJsonFile:mintKeypair|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "Calls mintKeypair/migratePlaintextEnvelope; the write is reported at its own site inside those functions."
    },
    "src/main/runtime/e2ee-secret-unseal-entry.ts|runE2EESecretHelperChild|fs:fs:writeSync|0": {
      "verdict": "fd-write-not-a-file",
      "reason": "writeSync(1, ...) \u2014 fd 1 is the pipe back to the parent process, not a file. This report models fd-level writes without a destination, so it cannot tell a pipe from a file; reviewed as a pipe. The module comment states the plaintext secret leaves only through this pipe."
    },
    "src/main/runtime/orca-runtime.ts|OrcaRuntimeService.linearConnect|wrapper:saveWorkspaceToken:connect|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "RPC/runtime entry point that calls the Jira/Linear connect flow; the token write is reported at its own site inside those clients."
    },
    "src/main/runtime/rpc/methods/jira.ts|JIRA_METHODS.handler|wrapper:connect:jiraConnect|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "RPC/runtime entry point that calls the Jira/Linear connect flow; the token write is reported at its own site inside those clients."
    },
    "src/main/runtime/rpc/methods/linear.ts|LINEAR_METHODS.handler|wrapper:connect:linearConnect|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "RPC/runtime entry point that calls the Jira/Linear connect flow; the token write is reported at its own site inside those clients."
    },
    "src/main/runtime/runtime-rpc.ts|OrcaRuntimeRpcServer.handleMessage|wrapper:emulatorProbe:dispatch|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/runtime/runtime-rpc.ts|OrcaRuntimeRpcServer.writeMetadata|wrapper:writeMetadataFile:writeRuntimeMetadata|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/skills/bundled-skill-swap-lock.ts|tryCreateSwapLock|file-handle:fs/promises.FileHandle:writeFile|0": {
      "verdict": "locally-minted-handshake-token",
      "reason": "A randomBytes(8) token Orca mints for a bundled-skill swap lock; it identifies the lock holder and must be readable at rest by the next process to check liveness."
    },
    "src/main/speech/openai-api-key-store.ts|saveOpenAiSpeechApiKey|fs:fs:writeFileSync|0": {
      "verdict": "encrypted-payload",
      "reason": "safeStorage ciphertext branch (.enc file). The plaintext branch at the next write in this function IS gated by allowsPlaintextPersistedSecret."
    },
    "src/main/ssh/ssh-remote-linear-list-issues.ts|dispatchRemoteLinearListIssues|wrapper:emulatorProbe:dispatch|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/ssh/ssh-remote-linear-read-cli.ts|call|wrapper:emulatorProbe:dispatch|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/ssh/ssh-remote-linear-write-support.ts|call|wrapper:emulatorProbe:dispatch|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/ssh/ssh-remote-orca-cli.ts|call|wrapper:emulatorProbe:dispatch|0": {
      "verdict": "orchestrator-forwarding",
      "reason": "A call into a larger routine that performs the write further down; that write is reported at its own site. Listed here so the forwarding call is visible too."
    },
    "src/main/startup/staging-profile-migration.ts|copyStagingSafeStorageKeychainItem|fs:fs:writeFileSync|0": {
      "verdict": "non-secret-payload",
      "reason": "Migration marker JSON: schema version, attempt count, outcome. The keychain secret is moved by the `security` CLI, never through this write."
    },
    "src/main/startup/startup-diagnostics.ts|writeStartupDiagnosticLine|escape:fs:writeSync|0": {
      "verdict": "fd-write-not-a-file",
      "reason": "ESCAPE, not a site: fs.writeSync is captured as the default value of the injectable StartupDiagnosticSink parameter, so calls through it are invisible to this report. Reviewed: the only call is write(2, line) \u2014 file descriptor 2, stderr \u2014 and the payload is a startup diagnostic string. Replacing the default with a wrapper function would remove the escape."
    },
    "src/relay/agent-hook-server.ts|RelayAgentHookServer.publishEndpointFile|wrapper:writeFileSync:writeEndpointFile|0": {
      "verdict": "locally-minted-handshake-token",
      "reason": "The agent-hook endpoint file publishes a per-run random loopback token that the agent CLI must source from a shell. Encrypting it would defeat its only purpose; it is short-lived, loopback-scoped and shell-safety validated before the write."
    },
    "src/shared/agent-hook-listener.ts|writeEndpointFile|fs:fs:writeFileSync|0": {
      "verdict": "locally-minted-handshake-token",
      "reason": "The agent-hook endpoint file publishes a per-run random loopback token that the agent CLI must source from a shell. Encrypting it would defeat its only purpose; it is short-lived, loopback-scoped and shell-safety validated before the write."
    }
  }
}
